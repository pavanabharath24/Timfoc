name: usage_stats_helper
description: Native UsageStats helper for Timfoc app locker
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

flutter:
  plugin:
    platforms:
      android:
        package: com.timfoc.usage_stats_helper
        pluginClass: UsageStatsHelperPlugin

dependencies:
  flutter:
    sdk: flutter
